> **Org rules:** read `CLAUDE.md` in `agentic-yankiz/agents-mgmt` — applies here unless explicitly overridden below.

<!-- END ORG HEADER -->

# ADE Example

This repository publishes a static, non-production Training ADE vision to GitHub Pages. Keep the site free of secrets and production data. GitHub operations use the official MCP server authenticated by `yankihermesapp`; never use PATs or agent-side `gh`.
